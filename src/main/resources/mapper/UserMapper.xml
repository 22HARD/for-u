<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.loki.btmanage.mapper.UserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.loki.btmanage.pojo.User">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="name" property="name" />
        <result column="enabled" property="enabled" />
        <result column="userFace" property="userFace" />
        <result column="remark" property="remark" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, username, password, name, enabled, userFace, remark
    </sql>

    <resultMap id="UserWithRole" type="com.loki.btmanage.pojo.User" extends="BaseResultMap">
        <collection property="roles" ofType="com.loki.btmanage.pojo.Role">
            <id column="rid" property="id" />
            <result column="rrname" property="rname" />
            <result column="rname" property="name" />
        </collection>
    </resultMap>

    <!--获取所有操作员-->
    <select id="getAllUsers" resultMap="UserWithRole">
        SELECT
        u.*,
        r.id AS rid,
        r.`rname` AS rrname,
        r.name AS rname
        FROM
        t_user u
        LEFT JOIN t_user_role ur ON u.id = ur.uid
        LEFT JOIN t_role r ON r.id = ur.rid
        WHERE
        u.id != #{id}
        <if test="null!=keywords and ''!=keywords">
            AND u.`name` LIKE CONCAT( '%', #{keywords}, '%' )
        </if>
        ORDER BY
        u.id
    </select>

</mapper>
